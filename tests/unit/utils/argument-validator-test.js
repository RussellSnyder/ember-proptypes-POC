import argumentValidator from 'button-arguments/utils/argument-validator';
import { module, test } from 'qunit';

module('Unit | Utility | argument-validator', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = argumentValidator();
    assert.ok(result);
  });
});
