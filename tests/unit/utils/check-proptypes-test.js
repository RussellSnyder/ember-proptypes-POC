import checkProptypes from 'button-arguments/utils/check-proptypes';
import { module, test } from 'qunit';

module('Unit | Utility | check-proptypes', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = checkProptypes();
    assert.ok(result);
  });
});
